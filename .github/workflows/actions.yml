name: C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubunto-latest
    steps:
      - uses: actions/checkout@v2
      - name: Compile C++ 2017
        run: g++ main.cpp -std=c++17 -o HellowWorld
